# 開発中メモ

## 凡例

- [ ] 未着手
- [/] 実装済み（未確認）
- [x] 確認済み

## Bug fix

- [x] ~~カスタムスクリプト要件がLLM推論に使われてない気がする~~
- [x] AIでスクリプトタイトルが生成されない（生成はされてるけどGUIに反映されない）
- [x] 動画スクリプトを生成する時、タイトルが既にあればタイトルも含めて推論させる（GUIでオンオフできるように）
- [x] tasksのscripts.jsonに動画のテーマやカスタムスクリプト要件や音声設定などが全く含まれていない。scripts.jsonには、それを生成したときのすべての情報を含めてほしい。
- [x] tasksのフォルダ名に使われる日時のタイムゾーンがJTCになってない。サーバTZはJTCなのに。コンフィグでタイムゾーン指定できるようにして
- [x] プリセットにVOICEVOX音声設定が含まれてない。サブ項目が漏れてるのでは？
- [x] 動画タイトルテキストにも色強調タグを適用して（動画スクリプト推論に動画テキストを使う時は色強調タグを消して渡すように忘れずに）
- [x] （もしかしたらscripts.jsonに関わるかもやけど）プリセットを保存すると動画のテーマ欄やカスタムスクリプト要件のテキストエリアの中身が消えるバグがある。
- [/] 2レイヤー合成の背景動画が完成動画に表示されない。背景動画素材も動画素材ソースから取得するようにして。カスタム背景にする場合はresource/bg_videoから選ぶでいいけど

## Tasks

- [x] llmcppでのローカルLLMに対応する
- [x] 使用プロバイダやモデル名やプロンプトなどをプリセット保存/読込できるようにする
- [x] 表示言語に日本語を追加する
- [x] 動画のcoverサイズの上下マージンを設定できるようにする（セーフマージンみたいに割合で指定）
- [x] 字幕と別で、画面上部に「タイトル」（15文字くらいで要約したキャッチーな文）
- [x] 文字のマージン（割合で）を指定できるようにする（動画のcoverサイズとは別）
- [x] テキストの一部をLLMによって青や赤や黄色に色分けして強調する
    - テキスト生成時にタグ付け済みテキストを生成させてもいいし、生成したテキストを後からLLMにタグ付け指示させてもいい（どっちがいいかな）
    - 色強調機能のオンオフ、強調色１，２，３の色選択もできるようにして
    - できれば、強調色は文字色だけでなく縁色もそれぞれに指定できるように
- [x] VOICEVOXやAivisSpeechにも対応する
- [x] storage/tasks/につくるフォルダ名のパターンを指定できるようにする（jinja形式で日付時刻やモデル名やシード値などを組み合わせられるようにして）
- [x] 動画のアスペクト比をcoverサイズにもできるようにする
- [x] 背景動画とメイン動画をそれぞれ付けれるようにする
    - 背景動画は抽象的な動画1本をずっとループ、coverサイズ
    - 手前動画はもとと同じく、テーマに合った動画、containサイズ
- [/] 使うGPUを選べるようにして（すでに選べる？）
- [ ] 台本作成時にWeb検索をして情報収集をするようにする
    - 指示役のLLMスレッドとタスク実行役LLMスレッドに分けて、今後Web検索以外のタスクもこなせるようにする
    - 複数実行タスクで複数箇所について検索することや、検索結果に基づいてさらに検索することも対応する
    - タスクの有無、タスクステップの最大値をGUIで設定できるようにする
    - タスクや推論の過程もtasks/scriptsに記録する
- [ ] llama.cppのモデルも配列で保存して、使ったことある（接続成功したことある）モデルはプルダウンで選びやすくする
- [ ] 一度DLした動画素材はタグも一緒に保存して、再利用できるようにする(もしもうそうなってるならそのままでいい)
- [ ] OTF、TTCフォントも使えるようにする
